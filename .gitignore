node_modules/
.DS_Store
*.log
img/_cand/
img/_cand2/

# MCP server config can carry API keys — never commit it to a public repo
.mcp.json
.claude/settings.local.json
.playwright-mcp/

# Firebase keys. Never commit these; see firebase/README.md.
google-services.json
tools/.venv-hf/

# On-device model weights: 3 GB, licensed, never committed.
models/

# Rendered deck previews. The .pptx is the artefact; these are throwaway.
deck-render/
# A throwaway virtualenv that carries a static ffmpeg for the deck's poster frames.
.venv-deck/

# The built deck. 129 MB with the three videos embedded, which is past what GitHub accepts —
# and it is an artefact anyway: tools/build_deck.py plus the videos reproduce it exactly.
ClashFit.pptx
